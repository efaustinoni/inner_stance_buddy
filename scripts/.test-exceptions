# .test-exceptions — files exempt from test coverage audit
# One filename per line (no path, no extension).
# Lines starting with # are comments.
#
# Every component and hook must have a test file.
# Only add entries here for auto-generated files, third-party wrappers,
# or files explicitly approved by the team — document the reason inline.
